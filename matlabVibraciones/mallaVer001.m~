filename = 'frame500-001.dat';
A = importdata(filename);


Z1 = A(:,3);
X1 = A(:, 1);
Y1 = A(:, 2);

xmax = max(X1);
xmin= min(X1);

x = xmin:1/10000:xmax;



%[X Y] = meshgrid(x, y);

%surface(X, Y, Z1)


%surface(X2, Y2, Z1Pk,'EdgeColor', 'none'), view(3)

%Sms = strcat('Zms-', S);
%figure('Name', Sms)
%mesh(X2, Y2, Z1Pk)
%


